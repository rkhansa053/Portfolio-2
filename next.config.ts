import { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: ["img.freepik.com", "encrypted-tbn0.gstatic.com", "www.shutterstock.com", "encrypted-tbn0.gstatic.com", "www.texttool.com", "images.pexels.com"], 
  },
};

export default nextConfig;
